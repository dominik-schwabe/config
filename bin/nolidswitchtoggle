#!/usr/bin/env bash

COMMAND="bash $HOME/bin/nolidswitch"

if pkill --exact -f "$COMMAND"; then
    notify-send -t 1000 -u low 'disable screen'
else
    $COMMAND &
    notify-send -t 1000 -u critical 'keep screen active'
fi
