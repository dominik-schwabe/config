#!/usr/bin/env bash

INSTANCE=$1
shift
CLASS=$1
shift
PROGRAMM=$@

IS_RUNNING=$(comm -12 \
  <(xdotool search --classname "$INSTANCE"  | sort) \
  <(xdotool search --class "$CLASS"  | sort))

if [ -z "$IS_RUNNING" ]; then
    $PROGRAMM &!
    exit 0
fi
exit 1
